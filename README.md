# BMNP Visitor App — Cloud APK Build

This project is prepared for a cloud Flutter build. The included `codemagic.yaml` builds a debug Android APK and exposes it as a downloadable artifact.

Recommended cloud builder: Codemagic.
